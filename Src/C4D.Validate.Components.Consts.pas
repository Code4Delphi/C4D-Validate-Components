unit C4D.Validate.Components.Consts;

interface

const
  TAG_MIN = '<min>';
  TAG_MAX = '<max>';
  TAG_FIELD_DISPLAY = '<FieldDisplay>';

  //pt-br
  MSG_DEFAULT_NOT_EMPTY_ptBR = 'Campo obrigatório sem preenchimento';
  MSG_DEFAULT_LENGTH_ptBR = 'Campo deve ter entre ' + TAG_MIN + ' e ' + TAG_MAX + ' caracteres';
  MSG_DEFAULT_LENGTH_MIN_ptBR = 'Campo deve ter ao menos ' + TAG_MIN + ' caracteres';
  MSG_DEFAULT_LENGTH_MAX_ptBR = 'Campo deve ter no máximo ' + TAG_MAX + ' caracteres';
  MSG_DEFAULT_VALUE_ptBR = 'Campo deve ter um valor entre ' + TAG_MIN + ' e ' + TAG_MAX + '';
  MSG_DEFAULT_VALUE_MIN_ptBR = 'Campo deve ter um valor mínimo de ' + TAG_MIN + '';
  MSG_DEFAULT_VALUE_MAX_ptBR = 'Campo deve ter um valor máximo de ' + TAG_MAX + '';
  MSG_DEFAULT_DATE_ptBR = 'Campo deve ter uma data entre ' + TAG_MIN + ' e ' + TAG_MAX + '';
  MSG_DEFAULT_DATE_MIN_ptBR = 'Campo deve ter uma data mínima de ' + TAG_MIN + '';
  MSG_DEFAULT_DATE_MAX_ptBR = 'Campo deve ter uma data máxima de ' + TAG_MAX + '';

  //en
  MSG_DEFAULT_NOT_EMPTY_enUS = 'Field cannot be empty';
  MSG_DEFAULT_LENGTH_enUS = 'Field must have between ' + TAG_MIN + ' and ' + TAG_MAX + ' characters';
  MSG_DEFAULT_LENGTH_MIN_enUS = 'Field must have at least ' + TAG_MIN + ' characters';
  MSG_DEFAULT_LENGTH_MAX_enUS = 'Field must have at most ' + TAG_MAX + ' characters';
  MSG_DEFAULT_VALUE_enUS = 'Field must have a value between ' + TAG_MIN + ' and ' + TAG_MAX + '';
  MSG_DEFAULT_VALUE_MIN_enUS = 'Field must have a minimum value of ' + TAG_MIN + '';
  MSG_DEFAULT_VALUE_MAX_enUS = 'Campo deve ter um valor máximo de ' + TAG_MAX + '';
  MSG_DEFAULT_DATE_enUS = 'Field must have a date between ' + TAG_MIN + ' and ' + TAG_MAX + '';
  MSG_DEFAULT_DATE_MIN_enUS = 'Field must have a minimum date of ' + TAG_MIN + '';
  MSG_DEFAULT_DATE_MAX_enUS = 'Field must have a maximum date of ' + TAG_MAX + '';

implementation

end.
